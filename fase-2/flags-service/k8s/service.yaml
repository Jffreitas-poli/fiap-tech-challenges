apiVersion: v1
kind: Service
metadata:
  name: flags-service
  namespace: toggle
spec:
  selector:
    app: flags
  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002
  type: ClusterIP
