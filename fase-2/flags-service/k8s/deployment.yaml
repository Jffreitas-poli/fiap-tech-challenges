apiVersion: apps/v1
kind: Deployment
metadata:
  name: flags-deployment
  namespace: toggle
  labels:
    app: flags
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: flags
  template:
    metadata:
      labels:
        app: flags
    spec:
      containers:
      - name: flags-container
        image: 047719652987.dkr.ecr.us-east-2.amazonaws.com/tech-challenge/flags-image:1.0
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
        resources:
          requests:
            memory: "64Mi"
            cpu: "20m"
          limits:
            memory: "128Mi"
            cpu: "40m"
        ports:
        - containerPort: 8002
        envFrom:
        - configMapRef:
            name: flags-configmap
        - secretRef:
            name: flags-secret
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 20
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 3
          successThreshold: 1
